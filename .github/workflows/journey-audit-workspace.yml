name: Journey audit workspace
on:
  push:
    branches: [feat/mobile-masterwork-journey]
    paths: [.github/workflows/journey-audit-workspace.yml]
permissions:
  contents: read
jobs:
  audit-workspace:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Snapshot tracked source only
        run: git archive --format=tar HEAD > /tmp/masterwork-source.tar
      - name: Export isolated audit source
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: masterwork-current-source
          path: /tmp/masterwork-source.tar
          retention-days: 1
          if-no-files-found: error
